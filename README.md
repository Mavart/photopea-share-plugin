# Photopea Share Plugin v4

Modifiche:
- interfaccia chiara;
- un solo pulsante: "Condividi immagine";
- nome file preso automaticamente da `app.activeDocument.name`;
- fallback popup / download se il browser blocca Web Share dentro iframe.

Sostituisci almeno `index.html` e `share.html` nel repository GitHub Pages.
